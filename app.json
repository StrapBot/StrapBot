{
  "name": "StrapBot",
  "description": "Deploy an instance of the bot on your Heroku instance!",
  "repository": "https://github.com/Ergastolator1/StrapBot",
  "env": {
    "TOKEN": {
      "description": "Your Discord bot's token.",
      "required": true
    },
    "MONGODB_URI": {
      "description": "Your MongoDB connection URI to selfhost data.",
      "required": true
    },
    "DEFAULT_LANGUAGE": {
        "description": "Your bot's default language. Can be `it` (italian) or `en` (english). Defaults to `en`",
        "required": false
    }
  }
}
